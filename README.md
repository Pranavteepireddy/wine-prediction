# wine-prediction
Predicting wine quality using PySpark on AWS EMR. The model is trained in parallel on EC2 instances, validated using F1 score, and deployed using Docker. Includes PySpark scripts, EMR setup instructions, and Docker deployment.
